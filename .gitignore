# ── Personal data (user fills these) ──
data/applications.md
data/pipeline.md
data/scan-history.tsv
data/scan-selection.json
data/scan-state.json
# data/cv-profiles.yml
data/deleted-applications.tsv
data/*.tsv
data/*.json
# Telemetrie generee (tokens, couts, generation ids) — jamais commitee.
data/*.jsonl
!data/.gitkeep

# Personal CV & interview prep
cv.md
# Ignore the content, ship the system-owned scaffold (README + .gitkeep).
interview-prep/*
!interview-prep/
!interview-prep/.gitkeep
!interview-prep/sessions/
interview-prep/sessions/*
!interview-prep/sessions/.gitkeep
!interview-prep/sessions/README.md

# Reports (generated per-application)
reports/*.md
!reports/.gitkeep

# Output (generated PDFs, HTML, CVs)
output/*
!output/.gitkeep

# Batch temp & state
batch/temp/*
batch/logs/*
!batch/logs/.gitkeep
batch/batch-state.tsv
batch/batch-input.tsv
batch/tracker-additions/*.tsv
batch/tracker-additions/merged/
!batch/tracker-additions/.gitkeep

# Job descriptions (scraped)
jds/*
!jds/.gitkeep

# ── User config (never auto-updated) ──
config/profile.yml
# portals.yml
modes/_profile.md
.update-dismissed
.update-lock

# ── Personal assets ──
# images/
# ui/covers/
ui/*.pdf

# ── Generated ──
.resolved-prompt-*
node_modules/
# Backup files: `{file}.bak` and `{file}.bak-{ISO timestamp}` shapes.
*.bak*

# ── OS ──
.DS_Store
# *.mov
# *.mp4

# ── IDE / Claude Code ──
.claude/settings.local.json
.claude/memory/
career-dashboard
.vscode/

# ── Environment / secrets ──
.env
.env.local
.vercel

# ── Auto-apply (runner Playwright) ──
scratch/
data/chrome-profile/
